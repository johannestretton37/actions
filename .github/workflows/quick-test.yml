name: Quick test

on:
  push:

jobs:
  first:
    runs-on: ubuntu-latest
    if: false
    steps:
      - name: First job
        run: echo "first"
  second:
    runs-on: ubuntu-latest
    needs: first
    # if: always()
    steps:
      - name: Second job
        run: echo "second"