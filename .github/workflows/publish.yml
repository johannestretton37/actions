name: Publish package

on:
  release:
    types:
      - "published"

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Publish to npm
        run: echo "Publishing ${{ github.event }}"